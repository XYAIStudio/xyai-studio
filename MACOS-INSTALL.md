# XYAI Studio — macOS 傻瓜安装教程（Apple Silicon）

> 适用版本：`v0.3.1-20260904-r2`  
> 安装包：`XYAI-Studio-0.3.1-arm64.dmg`（**未签名内测包**）  
> 适用电脑：**Apple 芯片**（M1 / M2 / M3 / M4 等 arm64）。**不支持**老款 Intel Mac。  
> 下载页：https://github.com/XYAIStudio/xyai-studio/releases/tag/v0.3.1-20260904-r2

本教程按「点哪里、输入什么」写，照着做即可。预计 5–10 分钟。

---

## 0. 安装前确认（30 秒）

1. 看一下你的 Mac 是否为 Apple 芯片：  
   左上角苹果菜单 → **关于本机** → 芯片一栏应写 **Apple M…**。  
   若写的是 **Intel**，请先不要装本 DMG，改用 Windows 包或等待 Intel 版。
2. 预留约 **1 GB** 磁盘空间（安装包约 430MB，安装后还会更大）。
3. 需要能访问 GitHub Releases（公司网络若拦 GitHub，请换网络或让同事代下）。

---

## 1. 下载安装包

1. 打开发布页：  
   https://github.com/XYAIStudio/xyai-studio/releases/tag/v0.3.1-20260904-r2
2. 在 **Assets（资源）** 列表里找到：  
   **`XYAI-Studio-0.3.1-arm64.dmg`**
3. 点击下载。文件一般会进「下载」文件夹。

可选（推荐给在意安全性的同学）：同一页再下载 **`SHA256SUMS.txt`**，用来核对文件没被改过（见文末「可选校验」）。

---

## 2. 打开 DMG（像开一个临时小磁盘）

1. 打开「访达」→「下载」。
2. 双击 **`XYAI-Studio-0.3.1-arm64.dmg`**。
3. 桌面或侧边栏会出现一个磁盘图标（名称里通常带 XYAI / Studio）。
4. 双击打开它，里面应能看到 **`XYAI Studio.app`**（有时旁边还有箭头指到「应用程序」）。

> 若双击 DMG 没反应：在文件上 **右键 → 打开**；或把文件拖到桌面再双击。

---

## 3. 安装到「应用程序」

**推荐做法（拖放）：**

1. 打开 DMG 窗口。
2. 把 **`XYAI Studio.app`** 拖到 **「应用程序」** 文件夹（Applications）。  
   - 若窗口里已有「应用程序」快捷方式，拖到那个图标上即可。  
   - 也可打开访达 → 左侧「应用程序」，再把 App 拖进去。
3. 等待复制完成（进度条走完）。
4. 安装完成后，可把左侧 DMG 磁盘图标拖到废纸篓「推出」。

**不要**长期从「下载」文件夹或 DMG 里直接天天运行（容易权限乱、更新也麻烦）。请放到「应用程序」。

---

## 4. 第一次打开：绕过「未签名」拦截（最关键，请慢慢做）

本包是 **内部试用、未做 Apple 开发者签名 / 未公证**。  
系统会提示「无法验证开发者」「已损坏」或「打不开」。这是预期现象，不是病毒提示本身。确认文件来自上面的官方 Release 后再继续。

### 方法 A（推荐，一次搞定）

1. 打开「终端」：  
   按 `Command + 空格` → 输入 `终端` 或 `Terminal` → 回车。
2. **整行复制**下面命令，粘贴到终端，回车：

```bash
xattr -cr "/Applications/XYAI Studio.app"
```

3. 若提示找不到文件，说明 App 不在「应用程序」里。改用（把路径换成你的实际位置）：

```bash
xattr -cr ~/Downloads/"XYAI Studio.app"
```

或先在访达里对 App **右键 → 按住 Option → 拷贝 “XYAI Studio.app” 作为路径名**，再写成：

```bash
xattr -cr "这里粘贴你的路径"
```

4. 关掉终端。

### 方法 B（图形界面，配合方法 A）

1. 打开「访达」→「应用程序」。
2. 找到 **XYAI Studio**，**右键（或 Control + 点击）→ 打开**。  
   （不要只双击；第一次请用右键「打开」。）
3. 弹出警告时点 **打开**。
4. 若只看到「完成 / 移到废纸篓」而没有「打开」：  
   打开 **系统设置 → 隐私与安全性** → 往下滚到「安全性」，点 **仍要打开**，再按提示确认。

### 若提示「已损坏，无法打开」

多数是隔离属性（quarantine）导致，**先做方法 A** 再右键打开。仍不行时：

1. 确认 App 在 `/Applications/XYAI Studio.app`。
2. 再执行一次方法 A。
3. 重启 Mac 后再右键「打开」一次。

---

## 5. 日常启动

以后可从以下任一入口打开：

- 「应用程序」里的 **XYAI Studio**
- Launchpad 里的图标
- Dock（可把图标拖到 Dock 固定）

首次启动可能稍慢（几秒到十几秒），属正常。

---

## 6. 启动后建议做什么

1. **登录 / 配置模型**  
   按界面提示配置要用的模型（云端或本地，以应用内说明为准）。
2. **网络**  
   需要连公司代理时，在系统网络设置里配好代理后再开应用。
3. **本地能力说明（请读）**  
   - 本包为 **未签名内测**（无 Apple 签名 / 未公证）。  
   - 已内置 **macOS arm64 Metal** 版 `llama-server`（llama.cpp）与 **XYOS** 运行所需依赖；本地 GGUF 需在应用内「模型广场」按硬件下载模型权重（权重不打进安装包）。  
   - 仅支持 **Apple 芯片**；推理后端为 Metal（不是 Windows 那套 Vulkan）。  
   - 云端模型与网络能力仍可照常使用。

---

## 7. 卸载（不需要时）

1. 退出 XYAI Studio（菜单栏 → XYAI Studio → 退出）。
2. 打开「应用程序」，把 **XYAI Studio** 拖到废纸篓。
3. （可选）清残留数据：访达菜单「前往 → 前往文件夹」，输入：

```text
~/Library/Application Support/
```

   若看到与 XYAI / DeepSeek Harness 相关的文件夹，确认不用后再删除。  
   **不确定就别删**，以免误删其他应用数据。

---

## 8. 常见问题（FAQ）

### Q1：双击没反应 / 闪一下就没了？
先做第 4 节方法 A，再用右键「打开」。仍不行：打开「控制台」App 看崩溃日志，或换重新下载 DMG。

### Q2：提示不是来自身份明确的开发者？
正常。本包未签名。确认来自本仓库 Release 后，用方法 A + 右键打开 /「仍要打开」。

### Q3：我是 Intel Mac 能装吗？
**不能**装当前 `…-arm64.dmg`。需要 Apple Silicon。

### Q4：能不能装到外置硬盘？
可以复制 App 到外置盘，但首次仍建议先装进本机「应用程序」验证能开，再迁移。

### Q5：公司电脑开了 MDM / 禁止未知开发者？
需要 IT 放行本应用，或使用已签名正式版（当前 Release 尚未提供 Apple 签名包）。个人机器按第 4 节即可。

### Q6：和 Windows 版功能一样吗？
界面与主流程尽量一致。本 Mac 包已带本地 GGUF 运行时（Metal）与 XYOS 依赖；Windows 的「简洁/专业/离线完整」分档安装方式不同，以各平台 Release 说明为准。

### Q7：下载很慢或失败？
换网络、关 VPN 试一次，或用能访问 GitHub 的环境下载后用隔空投送 / U 盘拷贝到目标 Mac。

---

## 9. 可选：校验文件完整性（SHA-256）

1. 下载同目录的 `SHA256SUMS.txt`。
2. 打开终端，进入下载目录（路径按你的实际修改）：

```bash
cd ~/Downloads
shasum -a 256 "XYAI-Studio-0.3.1-arm64.dmg"
```

3. 把输出的一长串哈希，和 `SHA256SUMS.txt` 里  
   `XYAI-Studio-0.3.1-arm64.dmg` 那一行比对，**必须完全一致**。

---

## 10. 一键速查（打印或发给同事）

| 步骤 | 做什么 |
| --- | --- |
| 1 | 确认是 Apple 芯片 Mac |
| 2 | 下载 `XYAI-Studio-0.3.1-arm64.dmg` |
| 3 | 双击 DMG，把 `XYAI Studio.app` 拖进「应用程序」 |
| 4 | 终端执行：`xattr -cr "/Applications/XYAI Studio.app"` |
| 5 | 访达中对 App **右键 → 打开** → 点「打开」 |
| 6 | 按应用内提示配置模型 |

发布页：https://github.com/XYAIStudio/xyai-studio/releases/tag/v0.3.1-20260904-r2  
直链（可能随版本更新）：https://github.com/XYAIStudio/xyai-studio/releases/download/v0.3.1-20260904-r2/XYAI-Studio-0.3.1-arm64.dmg

---

有安装卡点时，请记下：**系统版本**（关于本机）、**完整报错原文**、以及卡在第几步，便于排查。
