{
  "name": "@xyai/dsh-product-forge",
  "description": "XYAI Studio experimental Agent Forge product bundle: opt-in Cordis patch layer for agent-forge - never part of DESKTOP_PROFILE_BUNDLES or the default full xyai profile",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "files": [
    "cordis.patch.yml"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "dependencies": {
    "@xyai/dsh-agent-forge": "workspace:*"
  },
  "scripts": {
    "test": "vitest run --config vitest.config.ts"
  }
}