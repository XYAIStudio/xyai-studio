/**
 * @xyai/dsh-dev-shell — node half.
 *
 * Deliberately empty: the shell's session index / account bridge is carried by
 * DSH host services and the ai-employees plugin; the browser half surfaces the
 * dev-space entry points.
 */

/** Host plugin body — intentionally empty (package is a client UI plugin). */
export function apply(): void {}
