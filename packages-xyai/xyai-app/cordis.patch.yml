# @xyai/dsh-xyai-app - residual XYAI meta bundle stub (no plugin mounts).
#
# Offline-capable product layers: @xyai/dsh-product-base, @xyai/dsh-product-collab.
# Online commercial layer: @xyai/dsh-product-online.
# Experimental Agent Forge: @xyai/dsh-product-forge (explicit opt-in; not mounted
# here and not in DESKTOP_PROFILE_BUNDLES or the default profiles/xyai bundles).
#
# This patch intentionally inserts nothing so a forgotten profile dependency on
# xyai-app cannot re-enable forge by default.

[]