# components

## openxyos

Git submodule → https://github.com/XYAIStudio/openXYOS

```powershell
git submodule update --init --recursive components/openxyos
```

开发期追踪源码；正式运行仍须按 `OPENXYOS-PACKAGING.md` 产出独立 artifact 并做健康探针。源码 checkout ≠ 运行时已安装。