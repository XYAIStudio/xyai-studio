# 上下文交接（长时多轮）

## 目标

真流式、不限轮次、有长时记忆——像能一直聊下去的助手，而不是 40 条截断或整段回灌。

## 三层打包

| 层 | 内容 | 生命周期 |
| --- | --- | --- |
| 长期记忆 | `userData/memory/facts.json` 跨会话事实 | 持久 |
| 滚动摘要 | 旧轮次压缩成「会话迄今要点」 | 随会话持久 `userData/sessions/*.json` |
| 近期原文 | 最近约 12 轮完整对话 | 送进模型的工作集 |

完整 transcript **落盘不丢**；模型只吃打包窗口（默认约 24k 字符预算）。

## 资格线

- 默认：`engineMode: auto` → `ollama:*` 本机 NDJSON **真流式**（`runOllamaTurn`）
- 「本机流式对话」强制上述路径；仅设置「本机写文件」才连闲聊也走 `--oss`
- Harness 是增强层；缺二进制时软降级，不中断对话

## 交接原则

1. 冲突时以**近期对话 + 用户最新表述**为准  
2. 摘要用抽取式压缩（弱机也能做，不另起一次慢推理挡首 token）  
3. 偏好类句子启发式写入长期记忆（「我叫…」「请叫我…」「我喜欢…」）
