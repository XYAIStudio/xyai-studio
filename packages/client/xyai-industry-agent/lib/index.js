/**
 * Host-half anchor for the migrated, browser-only historical plugin.
 * The original user-facing implementation is supplied as lib/client.js
 * during the controlled runtime staging step.
 */
export const inject = []
export function apply() {}
