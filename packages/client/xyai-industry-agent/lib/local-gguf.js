/**
 * Stable package entry for the migrated local GGUF provider.
 *
 * The hashed implementation is the audited historical asset already vendored
 * with this package; this small entry keeps the Cordis profile independent of
 * a build-specific chunk filename.
 */
export { a as inject, n as apply, l as name } from './local-gguf-3IBAx29M.js'
